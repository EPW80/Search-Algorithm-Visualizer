version: '3.8'
services:
  web:
    build: .
    ports:
      - "80:80"
    restart: unless-stopped
    # Remove volumes since we're using nginx to serve static files
    # volumes:
    #   - .:/app
    #   - /app/node_modules
